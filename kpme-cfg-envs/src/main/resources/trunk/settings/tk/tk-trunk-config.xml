<config>
	<param name="rice.ksb.batch.mode">false</param>
	<!-- dev overrides -->
	<param name="override.finalapprover">true</param>
	<param name="local.workflow">true</param>
	<param name="no.save.document.on.create">false</param>
	<param name="dev.mode">false</param>
	<param name="message.persistence">true</param>
	<param name="message.delivery">synchronous</param>
	<!-- end dev overrides -->
	
	<param name="http.url">http://ci.kpme.kuali.org</param>
	<param name="application.url">${http.url}/kpme-${environment}</param>
	<param name="workflow.url">http://ci.kpme.kuali.org/kr-${environment}/kew/</param>
	<param name="serviceServletUrl">${http.url}/kpme-${environment}/remoting/</param>
	<param name="log4j.settings.path">${user.home}/kuali/main/shared/log4j_kpme.properties</param>
	
	<!-- database params -->
    <param name="tk.datasource.url">@kpme.dataSource.jdbcUrl@</param>
    <param name="tk.datasource.username">@kpme.dataSource.username@</param>
    <param name="tk.datasource.password">@kpme.dataSource.password@</param>
    
    <param name="kr.datasource.url">@kr.dataSource.jdbcUrl@</param>
    <param name="kr.datasource.username">@kr.dataSource.username@</param>
    <param name="kr.datasource.password">@kr.dataSource.password@</param>
	
    <param name="application.lookup.url">kr-${environment}/kr/lookup.do</param>
</config>